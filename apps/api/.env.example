# ── Server ──
PORT=3001
NODE_ENV=development

# ── Auth ──
JWT_SECRET=change-me-to-a-random-secret
COOKIE_DOMAIN=localhost

# ── SMTP (optional in dev – logs to console if missing) ──
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@stammkhatm.de

# ── Admin emails (comma‑separated) ──
ADMIN_EMAILS=admin@example.com

# ── App URL (used in emails) ──
APP_URL=http://localhost:5173
APP_URLS=

# ── Database ──
DATABASE_URL="postgresql://user:password@host:5432/stammkhatm"
